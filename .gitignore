# Dependencies
node_modules/

# Build output
dist/
dist-ssr/

# Rust / Tauri build artefacts (src-tauri has its own .gitignore too)
target/

# macOS
.DS_Store

# Logs and local env
*.log
npm-debug.log*
.env
.env.*
!.env.example

# Editor scratch
.vscode/
.idea/
*.swp

# Exported plans. The app writes these wherever it is asked to, and they are
# somebody's real data - this repo is public, so they must never be committed
# by an `add -A` that swept the working tree.
/timeline-*.json
*.timelime.json
